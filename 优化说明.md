# v0.4.0 翻译响应优化说明

## 代码审查结论与改动

原项目为 Chrome MV3 扩展，由 content.js 识别网页文本，background.js 调用本地 Ollama；默认模型 qwen3:0.6b。没有修改或替换模型权重、量化精度，也未把翻译改到云端。

| 环节 | 原实现 | 新实现 |
|---|---|---|
| 首段显示 | stream:false，等整批 16 段完成 | stream:true，收到完整 JSON 字符串项即显示；首批最多 2 段 |
| 批次大小 | 只按段落数，最长可累计 28800 字符 | 首批目标 600 字符，后续最多 1800 字符；单个长段落独占批次 |
| 后续批次 | 固定提取时的顺序 | 用户滚动后，对尚未开始的批次重新按视口距离排序 |
| 请求前检查 | 首次先 GET /api/tags 再 POST /api/show | 直接翻译；设置页保留有 8 秒超时的独立检查 |
| 模型加载 | 请求后保留 10 分钟 | 保留 30 分钟，减少后续冷加载；首次使用仍可能等待加载 |
| 缓存 | 仅 worker 内存，按模型和文本；容量清理不完整 | LRU，最多 800 项且估算大小不超过约 4 MB；session 存储跨 worker 重启复用 |
| 缓存隔离 | 切换接口/温度可能误命中 | 接口、模型、温度、提示词版本和原文共同作为键 |
| 多标签页 | 每页单独并发，无全局限制 | 所有页面共享并发队列；出队时复查缓存，默认并发 1 |
| 停止翻译 | 只设置布尔值，推理继续且可能回填 | 断开 Port，AbortController 取消 fetch，删除排队任务；generation 防止旧任务污染新任务 |
| 异常回退 | 除少数错误外都二分重试 | 只有模型结果格式/数量/长度错误可拆分；网络错误直接提示并暂停自动重试 |
| 长时间卡住 | 无超时 | 推理连续 90 秒无响应取消；Port 心跳、前端连接看门狗 |
| 自动模式 | 全局开关只触发首轮，后续仅域名开关生效 | 两种开关都能处理动态内容和滚动；移除后暂停，不自动把译文插回 |
| 动态原文 | 请求后不再核查 | 插入前核对原文，后续扫描清理已经变化或删除的来源 |
| 弹窗状态 | 可能收到其他标签页进度 | 按发送标签页过滤；显示首段、总耗时和缓存命中数 |

## 质量与实现边界

每段流式译文在最终 JSON 校验前是临时结果；如果最终数量、格式或完成状态异常，撤销本批非缓存译文，再按允许的错误类型重试。只在校验成功后写缓存。网络中断不能把未验证内容永久写入缓存。中文 Unicode、反斜杠和转义引号按完整 JSON 字符串解码；不会把半个字符串或半个转义直接显示给读者。

保留 think:false、temperature:0.1 和 4096 上下文。新增 num_predict:2048 限制异常长输出，达到上限时拆分批次；不把被截断的结果当成成功。删除原先统一剥离数字前缀的处理，避免错误删除原文中的编号。提示词使用输入 JSON 数组并明确不执行待翻译文本中的指令，但这不构成对模型翻译质量或抗提示注入能力的保证。

单段超过 1800 字符、代码块、输入框、隐藏区域、封闭 Shadow DOM、画布和图像仍不处理。小模型可能漏译、错译专业术语；结构化输出只能约束格式，不能证明语义正确。首批更小会增加请求数，首段通常可更早显示，但整页总吞吐可能随文本而变慢；请使用附带脚本和实际网页决定是否提高每批上限。没有开启后台长期定时预热；首次翻译承担必要的模型加载，之后使用 keep_alive 复用。

## 已完成验证

在 Node.js 环境执行 `node --test tests/*.test.cjs`：14 项通过、0 项失败。测试使用模拟 Ollama、Chrome API 和最小 DOM：

- 流式 NDJSON 分片、JSON 项逐段提取、Unicode 和转义字符。
- 自适应批次保留原顺序与条数，缓存按接口/模型/温度隔离。
- 跨标签页排队、排队后缓存复查、session 缓存恢复。
- 在途 fetch 取消、排队请求取消与重新请求。
- 模型 JSON、数量、长度限制异常，以及 HTTP/流中断错误分类。
- 重复文本的原始索引映射。
- 页面移除后立即重启，旧结果不回填；异常临时节点撤销及拆分重试。
- 推理期间原文变化不插入旧译文，列表扫描不重复，原文变化后可重译。

所有 JS/CJS 文件通过 Node 语法检查，manifest.json 可解析，引用文件均存在。测试输出保存在 `tests/test-results.txt`。

## 尚未完成的实机验证

本环境没有用户 Mac 或其 Ollama 模型，未测真实 tokens/s、Mac 首段时间、Metal 利用率或翻译准确率。云端浏览器策略拒绝本地测试页，未完成真实 Chrome 交互验证。附带 `tests/browser.html` 的 7 个浏览器交互场景供本地执行，不能把它视作已经通过的测试。实际扩展的 CORS、Port 与 MV3 后台休眠恢复仍需要在 Mac Chrome 上确认。

`tests/benchmark.cjs` 使用同一模型、同一 16 句英文、相同温度和上下文，对比原版批量请求与实际新版后台代码。它预热后交替测试三轮，另测缓存命中，并打印译文供人工核对。测试不包含网页 DOM 扫描、真实扩展 IPC 与绘制时间；缓存计时有约 5ms 轮询粒度。旧版基线仅复刻翻译 POST 的等待路径，不包含其额外连接检查。结果仅在用户本机运行后产生；当前没有宣称任何实测加速倍数。

## 升级步骤

1. 备份原文件夹，把优化包内的文件复制到原扩展目录（保留原 .git 目录和个人文件）。
2. 在 chrome://extensions/ 重新加载原扩展，确认版本 0.4.0。
3. 刷新待翻译网页；本次旧内容脚本与新 Port 协议不兼容，需刷新一次。
4. 初次使用保留默认每批 8 段、并发 1。扩展会将旧默认 16 段迁移为 8 段。
5. 在原英文网页观察弹窗的首段时间、总耗时和缓存数；在任务进行中测试“移除”和再次翻译。
6. 保持同一模型运行 `node tests/benchmark.cjs`，并人工比较译文，确认感知响应和总体耗时均符合使用需求。

使用原目录加载可保持扩展 ID 和原先 Ollama 专属授权。模型保留 30 分钟会继续占用内存；浏览器 session 缓存随浏览器退出清除，不写入 Chrome 同步存储。压缩包不含 .git 历史、模型或额外依赖。

## 接口依据

- Ollama Chat API：https://docs.ollama.com/api/chat
- Ollama 流式 NDJSON：https://docs.ollama.com/api/streaming
